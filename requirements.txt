# StaphScope - Python Requirements
# System dependencies must be installed separately:
# - BLAST+ (blastn, makeblastdb)
# - any2fasta
# - Perl 5+ with JSON module

# Core Python dependencies
biopython>=1.79
pandas>=1.3.0
click>=8.0.0

# Optional for future features
tqdm>=4.62.0
